# ============================================================================
# requirements.txt para la funcion serverless Python de Vercel (/api/index.py)
# ============================================================================
# IMPORTANTE: esta lista es DELIBERADAMENTE minima. El backend original en
# backend/requirements.txt tiene Celery, Redis, allauth social, axes,
# admin_honeypot, pytest, ruff, etc. — nada de eso se ejecuta en serverless.
#
# Para evitar reventar el limite de 50MB de Vercel Hobby, aqui solo
# incluimos lo imprescindible para servir la API REST.
# ============================================================================

# Core
Django==4.2.9
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-environ==0.11.2

# Postgres (psycopg2 binary para evitar compilar)
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Auth (JWT + djoser + dj-rest-auth)
djangorestframework-simplejwt==5.3.1
djoser==2.3.1
dj-rest-auth==5.0.2
django-allauth==0.60.1
PyJWT==2.8.0
argon2-cffi==23.1.0

# API utilities
drf-spectacular==0.27.0
django-filter==23.5
django-csp==3.8
django-bleach==3.1.0

# Static files (serverless-friendly)
whitenoise==6.6.0

# Encrypted fields para datos sensibles
django-encrypted-model-fields==0.6.5

# Utils minimas
python-dateutil==2.8.2
pytz==2023.3
python-json-logger==2.0.7
